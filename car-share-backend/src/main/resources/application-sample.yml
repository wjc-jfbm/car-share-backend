# ====================================
# 配置文件示例 — 复制为 application.yml 并修改
# ====================================

server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/car_share_db?useUnicode=true&characterEncoding=utf8&serverTimezone=GMT%2B8
    username: root
    password: your_password_here
  data:
    redis:
      host: localhost
      port: 6379

token:
  secret: your_jwt_secret_here

wechat:
  mini:
    appid: your_appid
    secret: your_secret
